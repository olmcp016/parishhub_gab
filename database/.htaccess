# Block all direct web access to this folder (schema/seed SQL dumps,
# hash-password.php) — the app never needs to serve anything from here.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
